# Plan: M2

Implement typed native/component/scenario records, a versioned valuation registry, deterministic driver and seven-component mappers, support-aware aggregation, separate realized reconstruction/evaluation, summaries, saveable artifacts and CLI. Constitution check PASS.
