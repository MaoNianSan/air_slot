# Round 2 M4 Manuscript Alignment

## Symbol consistency

- `C_k^{0,CU}(s)` is the M2 baseline consequence.
- `C_k^{a,CU}(s)` is the M3 action-conditioned consequence.
- `L_k^{a,m}(s)=f_k^m(C_k^{a,CU}(s))` is the M4 component loss under monetary system `m`.
- `L^{a,m}(s)=sum_k L_k^{a,m}(s)` is defined only when the declared monetary scope is fully mapped and supported.
- `J^m(a)` is a versioned residual-risk objective over the preserved scenario distribution.

M4 must never reuse `C^{0,CU}` for a non-A00 action. A00 alone maps the baseline because M3 enforces the identity.

## Claim scope

The manuscript may state that the engineering interface supports multi-system monetary interpretation, weighted expectation/variance/VaR/CVaR, provenance-complete residual-risk envelopes, and separate authoritative/conditional rankings. It must also state that the repository currently has no scientifically frozen production monetary mapping or approved tail/objective policy.

The manuscript may not describe test scales as RMB costs, scenario assumptions as supported effectiveness, conditional rankings as recommendations, or deterministic sorting as optimization. No current engineering test supports a real monetary saving, operational superiority, or scientific ranking claim.

## Uncertainty preservation

M4 receives every M3 scenario and original weight. Monetary conversion occurs per component and per scenario before aggregation. Weighted tail mass is normalized explicitly, including fractional mass at the VaR boundary. No top-1 scenario, equal-weight substitution, or pre-M4 averaging is allowed.

## Missing definitions before promotion

Production text still requires an approved monetary system and component mapping sources, frozen parameter versions, mapping coverage, positive-tail support policy, CVaR alpha, risk-objective coefficients, and the intended authority of reference-based responses.

No TeX or manuscript source was edited.
