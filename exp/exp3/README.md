# exp3

Ablations are transformed copies of frozen consequence/candidate artifacts. They do not retrain,
mutate formal inputs, or imply paper eligibility. Exp3 orchestration is present; the LLM audit
remains explicitly `NOT_RUN` and no Final Test run is generated. M3 registry hashing and CLI
closure are complete, but 22 non-A00 response-parameter templates remain `NOT_FROZEN`, so
`EXP3_READINESS` is `PARTIAL`.
