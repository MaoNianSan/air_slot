# M3-M4 Interface

`instantiate_candidates(pre_state, registry)`; `evaluate_decision(pre, m1_scenarios, m2_consequences, candidates, config)`; `project_authority(decision,user_capabilities)`. CLI: `instantiate-smoke`, `decide-smoke`, `validate`.
