# M2 Interface

`M2Mapper.map_scenarios(scenarios, pre_context, references)` returns scenario-level seven-component rows. `reconstruct_realized` is evaluation-only. CLI provides `map-smoke` and `validate`.
