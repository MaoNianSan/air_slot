# Plan: M1 Probabilistic Operational State

Use PyTorch on the current Python 3.11 interpreter. Implement PRE-to-tensor construction, immutable bin/feature contracts, one-layer GRU with ordered conditional heads, interval/exact episode-weighted objective, temperature calibration, stable aligned categorical sampling, artifact save/load, inference summaries and evaluation metrics. No virtual environment, raw reads, second fallback predictor, or dataset-name branches.

Constitution check: PASS. PRE remains the evidence authority; M1 support cannot exceed PRE; formal scenarios remain separate from evaluation metrics.
