# Re-verification After Defect Fixes

Date: 2026-08-02
Trigger: cleanup/documentation phase — the on-file audit (18:21) predated defect
fixes in `m3.py` (18:58), `predecessor_matcher.py` (18:55), and `m4_evaluation.py`
(20:03). A full re-verification was requested before updating README.

REVERIFICATION_STATUS=PASS
FAULT_INJECTION=15/15_PASS
TEST_SUITE=272/272_PASS
CODE_CORRECTNESS_CONFIRMED=YES

## 1. Fault injection probe (dynamic)

Command: `D:/Python311/python.exe reports/code_audit_fault_probe.py`

Result: `FAULT_INJECTION_STATUS=PASS`, 15/15 PASS, 0 FAIL.

All 15 active injections now pass, including every previously failing case:

| Injection | Detection mechanism |
|---|---|
| Overlapping immediate leg masks older valid predecessor | M1 predecessor backtracking (`valid_old` at depth 2) |
| Train/inference feature order swapped | M1 feature contract `M1_INFERENCE_FEATURE_ORDER_MISMATCH` |
| M3 boolean `"false"` supplied as string | `M3_BOOLEAN_FIELD_INVALID:A11:capacity_required` |
| A00 duplicated in formal action inventory | `M3_DUPLICATE_ACTION_ID` |
| Formal action count 26→25 | `ACTION_LIBRARY_MISMATCH` |
| M4 score tie secondary field | `expected_residual` orders A12 before A11 |
| Zero-candidate ranking boundary | 11 null padding rows for K=1/2/3/5 |
| Padding action_id set to A00 | `RANKING_PADDING_ACTION_NON_NULL` |
| overall_adv Global/Local candidate mismatch | `CandidateSetContractError` |
| Unknown PRE override field | `UNKNOWN_CONFIG_FIELD=predecessor_matching.foo` |
| R3 threshold in seconds | PRE config validator rejection |

Artifacts: `FAULT_INJECTION_RESULTS.csv`, `FAULT_INJECTION_RESULTS.json`,
`FAULT_INJECTION_AUDIT.md` (all regenerated by the probe).

## 2. Full test suite

Per-module invocation (each module run from its own directory with
`python -m pytest`):

| Suite | Result |
|---|---|
| `pre/tests/` | 29 passed |
| `overall_run/tests/` | 109 passed |
| `overall_adv/tests/` | 18 passed |
| `part_adv/tests/` | 16 passed |
| root `tests/` (5 files) | 82 passed |
| `tests/p1_event_reconstruction/` | 18 passed |
| **Total** | **272 passed, 0 failed** |

Note: running `pytest` from the repo root fails collection due to duplicate test
module basenames without `__init__.py`; the authoritative invocation is per-module
as above (matches how the 227-test baseline was measured).

## 3. Per-finding code verification

| Original finding | Current state | Evidence |
|---|---|---|
| M1 `shift(1)` misses valid predecessor past overlap | FIXED | matcher iterates candidates, selects latest valid non-overlapping predecessor; fault + 11 tests |
| M1 feature-order parity missing | FIXED | `M1FeatureContract.validate_feature_frame` rejects order mutation |
| M3 string booleans unsafe | FIXED | strict `M3_BOOLEAN_FIELD_INVALID` checks in `m3.py` |
| M3 multiple action authorities | FIXED | single loader `overall_run/src/action_contract.py`; `m3.py` uses it |
| M4 tie-break uses implementation cost | FIXED | sort `score, expected_residual, priority, action_id` in `m4_evaluation.py` and shared `ranking_contract` |
| M4 zero-candidate padding unsupported | FIXED | `ranking_contract.py` emits fixed-width null padding; validator rejects non-null padding |
| overall_adv candidate-set mismatch accepted | FIXED | `validate_candidate_sets` raises `CandidateSetContractError` (pipeline_analysis.py:198) |
| Ranking depths duplicated in metadata | FIXED | imported from shared contract (`pipeline_finalize.py:23`, `part_adv/src/pipeline.py:11`) |
| clean cannot target dev output | FIXED | `clean.py --output-id fast_three_change_dev` dry-run works (all 4 modules) |

## 4. Honest caveats

- **M3_ACTION_REACHABILITY=PARTIAL**: synthetic positive/negative reachability tests
  pass (`test_m3_v3_expanded.py`), but 13 formal actions remain zero-scored in the
  real fast output. Explicit resolution of those actions is required before scientific
  parameter approval.
- **DEV_FAST_CHAIN_STATUS=PARTIAL**: clean isolation is now possible, but the full
  development chain was not re-run after the fixes; the existing dev output is
  partial/interrupted and scheduled for cleanup.
- **Parallel determinism**: PASS measured earlier (63 parquets / 6 registries). The
  14-thread dev output has since been removed, so this cannot be re-run without
  re-executing the chain.
- **Formal fast / middle / full**: not re-run after fixes.
- **R3 / M3 parameter review**: PENDING.
