# Exp2 Remaining Scientific Gate

Status: `M1_M2_REAL_ARTIFACT_BLOCKED`

The earliest remaining execution blocker is a train-frozen executable M1 V2
checkpoint and scenario artifact for the current primitive chain
`T_IB_A00 -> D_OB -> D_TX`, with derived `R_IB` and `D_TO`. No historical V1
warning/scenario artifact may substitute for it.

After that freeze, the next required stage is M2 V2 consequence materialization
with exact M1 scenario IDs, weights, reference lineage, and explicit abstains.
Only then can Exp2A representation transformations, Exp2B coarsenings, the
predeclared common action set, and Development-only predictive checks be
materialized.

Two downstream gates remain separate: M1 positive-tail support for
`CVaR_0.90`, and a complete seven-component frozen monetary mapping. Until
both close, M4 risk execution and all action/risk metrics remain abstained.

No Final Test data were read and no paper-scale or scientific Exp2 run was
performed.
