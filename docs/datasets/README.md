# Dataset roots

Configured data1 and data2 roots are external, read-only inputs. Project metadata belongs under
`metadata/datasets/`; no source, cache, registry, README, or validation artifact may be written to a
raw-data root.
