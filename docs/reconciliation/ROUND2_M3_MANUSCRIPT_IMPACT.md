# Round 2 M3 Manuscript Impact

## Future sections affected

When manuscript editing is authorized, the method overview should place M3 after M2 baseline construction and before M4 valuation. The action-space subsection should distinguish registry membership, eligibility, and response support. The response-model subsection should replace any universal scalar-response description with typed mechanisms and explicit support classes. The uncertainty subsection should state that scenario identities and weights survive M3 unchanged. The limitations subsection should disclose the scenario-only status and disabled V2 execution of all non-A00 responses.

## Required replacements

Any use of `P(a)` for structural feasibility should be replaced by `I(a)`; `P(a)` is reserved for response. Any statement that M3 minimizes or selects should be assigned to M4 or removed. Any generic “response parameters are frozen” statement must say whether this means version-frozen scenario parameters or scientifically supported parameters. Any mean-only action consequence should be replaced by the all-scenario envelope.

## Evidence boundary

The 23-action matrix is an interface/design inventory. It is not evidence of effectiveness. The A00 identity is an engineering invariant. The A–J tests validate serialization, separation, abstention, lineage, scenario preservation, and absence of monetary fields; they do not validate recovery outcomes.

## Human decisions required before manuscript promotion

1. Approve or replace the legacy non-A00 scenario tiers and the component-wise transformation for each response type.
2. Decide which actions can be upgraded to `REFERENCE_BASED` or `SUPPORTED`, with source references and frozen parameter versions.
3. Decide whether a dedicated response source may construct an action-conditioned value when M2 baseline CU is unavailable; current V2 conservatively preserves abstention.
4. Approve the precise M4 expectation/variance/tail and monetary-ranking definitions after this M3 interface.

No manuscript/TeX file was changed. No experiment, optimizer, ranking, monetary calibration, or scientific-support promotion was run.
