# Research Decisions

Because action logs are unsupported, all non-A00 response parameters are bounded scenario assumptions unless a stronger frozen source is explicitly present. A00 is deterministic FULL coverage. UNKNOWN preconditions retain candidates but prevent FORMAL lane. Weighted CVaR uses the worst alpha tail with fractional boundary weight.
