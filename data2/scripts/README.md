# Non-runtime data repair utilities

These one-off audit and repair scripts are retained with the Data2 source
materials for provenance. They are not imported by the production runtime and
are outside the production code-size audit scope. Each script resolves the
Data2 root from its own location; no machine-specific absolute root is used.
