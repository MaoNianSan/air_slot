# Research Decisions

Valuation functions are auditable linear/piecewise rules from explicit frozen parameters. Undefined parameters block affected components. Formal totals require all configured critical components; available sum is diagnostic. Scenario summaries never replace long-form output.
