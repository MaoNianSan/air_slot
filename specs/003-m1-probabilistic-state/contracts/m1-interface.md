# M1 Interface

`M1Pipeline.fit`, `calibrate`, `predict_distributions`, `sample_aligned`, `save`, and `load` accept PRE-derived tensors/contracts only. CLI: `python -m model.M1.cli train-smoke`, `infer-smoke`, and `validate`.
