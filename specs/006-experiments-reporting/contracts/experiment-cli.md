# Experiment CLI
`python -m exp.cli run --experiment exp1|exp2|exp3|exp4 --input <formal> --output <evaluation> [--smoke]`; `report`; `promote`; `validate`. Smoke artifacts have `paper_result=false` and cannot promote.
